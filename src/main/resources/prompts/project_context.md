directoryStructure: Below is a snapshot of this project's Android module structure at the start of the conversation, derived from `settings.gradle(.kts)` includes. This snapshot will NOT update during the conversation.

{structure}

Absolute path: {abs_path}
