<idea-plugin require-restart="true">
    <id>com.song.smoker</id>
    <name>Smoker</name>
    <vendor>com.song</vendor>
    <description><![CDATA[
        Code-smell refactoring agent triggered from a side tool window.
        Open an Android project, expand the Smoker tool window on the right,
        click "Run Smoker" to: pull develop → create a new branch → apply
        IDE inspection quick-fixes → apply detekt quick-fixes → run the
        Android Lint LLM agent → push and open a PR.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.kotlin</depends>
    <depends>org.jetbrains.android</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="DetektAgent"
                    anchor="right"
                    secondary="false"
                    factoryClass="com.song.smoker.detektagent.ui.DetektAgentToolWindowFactory"/>
        <postStartupActivity implementation="com.song.smoker.daemon.SmokerHttpListenerActivity"/>
    </extensions>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK2="true"/>
    </extensions>
</idea-plugin>
